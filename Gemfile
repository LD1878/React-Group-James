source "https://rubygems.org"

# This locks your local environment to GitHub Pages versions
gem "github-pages", group: :jekyll_plugins

# The specific SEO plugin we added to _config.yml
gem "jekyll-seo-tag"

# Required if you are running Ruby 3.0+ on your machine
gem "webrick"
